from util import slice_first_byte
from CONSTANTS import TYPES
# lets make a naive parser first

from util import slice_first_byte


def parse(data: bytes):
    _data = data.split(b"\r\n")
    atoms = []
    for i in _data:



    
        
